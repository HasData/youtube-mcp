[project]
name = "hasdata-youtube-mcp"
version = "1.0.1"
description = "MCP server for YouTube data through HasData's hosted API. No Google Cloud project, no YouTube Data API key."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["mcp", "youtube", "model-context-protocol", "hasdata"]
# mcp is pinned below 2 because mcp-proxy 0.12.0 imports request_ctx, removed in the mcp 2.x SDK.
dependencies = ["mcp-proxy>=0.12.0", "mcp>=1.17,<2"]

[project.urls]
Homepage = "https://hasdata.com/apis/youtube-scraper-api"
Repository = "https://github.com/HasData/youtube-mcp"

[project.scripts]
hasdata-youtube-mcp = "hasdata_youtube_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hasdata_youtube_mcp"]
